#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1


CWD=`pwd`

cd $BUILD/libogg* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --disable-shared && \
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
