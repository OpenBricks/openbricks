#!/bin/sh

. config/options

$SCRIPTS/build toolchain

unset INSTALL

cd $BUILD/libogg*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared
make
make install
