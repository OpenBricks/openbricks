#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build ncurses

cd $BUILD/dialog*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --disable-nls \
            --without-dbmalloc \
            --without-dmalloc \
            --with-ncurses \
            --disable-widec \
            --disable-rc-file \
            --disable-Xdialog \
            --disable-form \
            --disable-tailbox
make
