#!/bin/sh

. config/options

get_meta $1

mkdir -p $INSTALL/etc/X11/

for drv in $PKG_DEPENDS; do
  echo $drv | grep -q input && continue
  echo "$drv" | sed 's/.*xf86-video-//' >> $INSTALL/etc/X11/drivers
done
