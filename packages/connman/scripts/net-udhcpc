#!/bin/sh

. /etc/network
. /etc/network-backend

[ "$NET_B" != udhcpc ] && exit 1

ifconfig $IFACE up

[ -n "$SSID" ] && iwconfig $IFACE essid $SSID key $PASSPHRASE
udhcpc -s /bin/assign-ip -i $IFACE

exit 0
