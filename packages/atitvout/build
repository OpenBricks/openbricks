#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd "`ls -d $BUILD/atitvout*`" && \
make CC=$TARGET_CC && \
$TARGET_STRIP atitvout && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
