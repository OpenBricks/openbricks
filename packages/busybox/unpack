#!/bin/sh

. config/path

$SCRIPTS/unpack uClibc || exit 1


CC="`ls -d $ROOT/$BUILD/uClibc*/`build/usr/bin/"

sed "s%CROSS_COMPILER_PREFIX=.*%CROSS_COMPILER_PREFIX=\"$CC\"%" $PACKAGES/busybox/busybox.conf > "`ls -d $BUILD/busybox*/`.config"
