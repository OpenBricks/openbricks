#!/bin/sh

. config/options

$SCRIPTS/build toolchain

sed "s%CROSS_COMPILER_PREFIX=.*%CROSS_COMPILER_PREFIX=\"$TARGET_PREFIX\"%" $PACKAGES/busybox/config/busybox.conf > "`ls -d $BUILD/busybox*/`.config"
