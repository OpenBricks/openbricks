#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

sed "s%CROSS_COMPILER_PREFIX=.*%CROSS_COMPILER_PREFIX=\"$TARGET_PREFIX\"%" $PACKAGES/busybox/busybox.conf > "`ls -d $BUILD/busybox*/`.config"
