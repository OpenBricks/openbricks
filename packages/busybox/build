#!/bin/sh

. config/path

$SCRIPTS/build uClibc || exit 1


export UCLIBC_GCC_DLOPT="-Wl,--dynamic-linker,/lib/ld-uClibc.so.0"
CWD=`pwd`

cd "`ls -d $BUILD/busybox*`" && \
make dep && \
echo '#define CONFIG_USE_BB_SHADOW 1' >> include/config.h
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
