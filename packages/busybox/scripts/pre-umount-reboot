#!/bin/sh

while read i ; do 
  umount `echo $i | cut -d" " -f1`
done < /var/mnts 

while read i ; do 
  a=`echo $i | grep "/mnt/shares/"| cut -d" " -f2`
  [ -n "$a" ] && umount $a 
done < /proc/mounts

systemctl reboot
