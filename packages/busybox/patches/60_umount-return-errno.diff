diff -Naur busybox-1.10.0.orig/util-linux/umount.c busybox-1.10.0/util-linux/umount.c
--- busybox-1.10.0.orig/util-linux/umount.c	2008-04-05 17:16:16.000000000 +0200
+++ busybox-1.10.0/util-linux/umount.c	2008-04-05 17:17:35.000000000 +0200
@@ -34,7 +34,7 @@
 	struct mntent me;
 	FILE *fp;
 	char *fstype = NULL;
-	int status = EXIT_SUCCESS;
+	int status = 0;
 	unsigned opt;
 	struct mtab_list {
 		char *dir;
@@ -127,7 +127,7 @@
 				}
 				bb_error_msg(msg, m->device);
 			} else {
-				status = EXIT_FAILURE;
+				status = errno ? errno : 127;
 				bb_perror_msg("cannot %sumount %s", (doForce ? "forcibly " : ""), zapit);
 			}
 		} else {
