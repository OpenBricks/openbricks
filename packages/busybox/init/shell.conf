description  "Start debug shell"
author       "GeeXboX"

start on startup and persistent-data
respawn

pre-start script
  grep "configure" /var/runlevel && exit 1
  exit 0
end script

exec /sbin/getty -n -l /bin/sh 38400 tty1
