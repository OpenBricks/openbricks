#!/bin/sh

. config/options

if [ "$VDPAU" = yes ]; then
  echo "## VDPAU enabled, skipping MT decoding patch"
else
  echo "## Applying MT decoding patch"
  cat $PACKAGES/$1/patches.mt/10_mt-decode.diff | patch -d \
    `echo $BUILD/$1* | cut -f1 -d\ ` -p1
fi
