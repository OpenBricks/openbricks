#!/bin/sh

. config/options

MOD_DIR="`ls -d $INSTALL/lib/modules/*`/kernel/extra"

mkdir -p ${MOD_DIR}
for m in pvrsrvkm omaplfb bc_example; do
  cp $BUILD/$1*/eurasiacon/binary_omap3430_linux_release/${m}.ko $MOD_DIR
done
