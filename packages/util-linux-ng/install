#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin $INSTALL/sbin

if [ "$2" = "blkid" ]; then
  cp $BUILD/$1-*/misc-utils/blkid $INSTALL/sbin
else
  cp $BUILD/$1-*/fdisk/sfdisk $INSTALL/usr/bin
fi
