#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin

cp $BUILD/$1*/fdisk/sfdisk $INSTALL/usr/bin
cp -P $BUILD/$1*/shlibs/uuid/src/.libs/libuuid.so* $LIB_PREFIX/lib
cp -P $BUILD/$1*/shlibs/blkid/src/blkid.h $LIB_PREFIX/include 
