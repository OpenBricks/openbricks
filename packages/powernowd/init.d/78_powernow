#!/bin/sh
#
# powernow deamon
#
# runlevels: geexbox, debug, configure

if [ -x /usr/bin/powernowd \
     -a -f /sys/devices/system/cpu/cpu0/cpufreq/cpuinfo_max_freq ]; then
  echo "### Starting PowerNow! daemon ###"
  powernowd >/dev/null 2>&1
fi

exit 0
