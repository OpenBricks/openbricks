#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

unset INSTALL

cd $BUILD/libdts* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX && \
make && \
make install && \
exit 0

exit 1
