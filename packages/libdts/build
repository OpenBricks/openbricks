#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/libdts* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME && \
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
