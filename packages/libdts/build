#!/bin/sh

. config/options

$SCRIPTS/build toolchain

unset INSTALL

cd $BUILD/libdts*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX
make
make install
