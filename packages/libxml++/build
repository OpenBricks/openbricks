#!/bin/sh

. config/options

require_cxx $1

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --disable-static \
            --enable-shared \
            --disable-dependency-tracking \

make
make_install
