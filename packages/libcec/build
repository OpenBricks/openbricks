#!/bin/sh

. config/options
get_meta $1

cd $PKG_BUILD_DIR

autoreconf -vif

if [ "$TARGET_MACHINE" = cubox ]
  then do_configure --enable-cubox --with-tda995x-toolkit-path=$(kernel_path)/drivers/video/dovefb/nxp_hdmi
  else do_configure
fi

make
make_install
