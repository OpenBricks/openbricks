#!/bin/sh

. config/options
get_meta $1

cd $PKG_BUILD_DIR

autoreconf -vif
[ "$TARGET_MACHINE" = cubox ] && do_configure --enable-cubox --with-nxp-toolkit-path=$(kernel_path)/drivers/video/dovefb/nxp_hdmi || do_configure

make
make_install
