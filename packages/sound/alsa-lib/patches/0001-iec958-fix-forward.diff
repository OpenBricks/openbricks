--- a/src/pcm/pcm_iec958.c	2017-11-04 11:19:18.055326828 +0100
+++ b/src/pcm/pcm_iec958.c	2017-11-04 11:19:18.103326450 +0100
@@ -436,7 +436,7 @@
 {
 	unsigned int counter_increment;
 	snd_pcm_iec958_t *iec = pcm->private_data;
-	snd_pcm_sframes_t result = snd_pcm_plugin_rewind(pcm, frames);
+	snd_pcm_sframes_t result = snd_pcm_plugin_forward(pcm, frames);
 	if (result <= 0)
 		return result;
 
