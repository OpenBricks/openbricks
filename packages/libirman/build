#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`
export CC=$TARGET_CC
export CFLAGS="-Os"

cd $BUILD/libirman* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME && \
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
