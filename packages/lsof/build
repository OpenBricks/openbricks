#!/bin/sh

. config/options

$SCRIPTS/build toolchain

KVERSION=`ls -d $BUILD/linux-* | cut -d\- -f2-`
cd $BUILD/$1_*/$1_*src
LSOF_VSTR=$KVERSION LINUX_HASSELINUX=n LINUX_BASE=/proc ./Configure -n linux

make CC="$CC" CFGL="$LDFLAGS -L./lib -llsof"
