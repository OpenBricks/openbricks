#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build MPlayer
$SCRIPTS/build xine-lib

if [ "$XORG" = yes ]; then
  XORG_CONFIG="--enable-x11"
else
  XORG_CONFIG="--disable-x11"
fi

cd $BUILD/$1*
./configure --prefix=$LIB_PREFIX \
            --disable-static \
            --enable-shared \
            --enable-mplayer \
            --enable-xine \
            --disable-vlc \
            --disable-gstreamer \
            --disable-debug \
            --cross-compile \
            --disable-doc \
            --enable-logcolor \
            $XORG_CONFIG

make -C src
make -C src install
cp -f libplayer.pc $LIB_PREFIX/lib/pkgconfig/
