#!/bin/sh

. config/options

cd $BUILD/$1*/wpa_supplicant

WPA_SUPPLICANT=`ls -d $BUILD/$1*`/wpa_supplicant

cp $PACKAGES/$1/config/makefile.config $WPA_SUPPLICANT/.config

if [ "$DEBUG" = no ]; then
  echo "CONFIG_NO_STDOUT_DEBUG=y" >> $WPA_SUPPLICANT/.config
fi
