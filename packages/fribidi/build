#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/fribidi*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared
make
make install
