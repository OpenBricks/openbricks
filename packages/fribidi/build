#!/bin/sh

. config/options

cd $BUILD/$1*
do_configure \
            --disable-debug \
            --enable-malloc \
            --with-glib=no \

make
make_install
cp -f $ROOT/$PACKAGES/$1/scripts/$1-config .install/usr/bin/$1-config
