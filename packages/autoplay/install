#!/bin/sh

. config/options

mkdir -p $INSTALL/etc
cp $PACKAGES/$1/config/autoplay.conf $INSTALL/etc

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/$1 $INSTALL/usr/bin
cp $PACKAGES/$1/scripts/autoplay_update $INSTALL/usr/bin

exit 0
