#!/bin/sh

. config/options

cd $BUILD/$1*

do_install usr/lib/xorg/modules/drivers/*_drv.so

mkdir -p $INSTALL/usr/lib/xorg/gpu
cp -P $ROOT/$PACKAGES/$1/scripts/gpu $INSTALL/usr/lib/xorg/gpu/mali.sh
