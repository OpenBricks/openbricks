#!/bin/sh

. config/options

installdir="$INSTALL/lib/modules/$(kernel_version)/misc"
mkdir -p $installdir
cp $BUILD/$1*/driver/ndiswrapper.ko $installdir

mkdir -p $INSTALL/sbin
cp $BUILD/$1*/utils/loadndisdriver $INSTALL/sbin
