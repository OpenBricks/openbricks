#!/bin/sh
. config/options

cp $BUILD/ndiswrapper*/driver/ndiswrapper.ko $INSTALL/lib/modules/*/misc

mkdir -p $INSTALL/sbin
cp $BUILD/$1*/utils/loadndisdriver $INSTALL/sbin
cp $PACKAGES/$1/scripts/ndiswrapper_convert $INSTALL/sbin
