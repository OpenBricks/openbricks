#!/bin/sh

. config/options
get_meta $1

cd $PKG_BUILD_DIR

rm -rf .install
mkdir -p .install/firmware/vpu

if [ -d firmware/vpu ] ; then
  cp -a firmware/vpu/vpu_fw_imx6*.bin .install/firmware/vpu
else
  cp -a firmware/* .install/firmware/vpu
fi
