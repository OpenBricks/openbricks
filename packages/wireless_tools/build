#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build linux

make -C $BUILD/wireless_tools* \
     PREFIX=/usr \
     KERNEL_SRC=$KERNEL \
     CC=$CC \
     AR=$AR \
     RANLIB=$RANLIB \
     CFLAGS="$CFLAGS -I." \
     BUILD_STATIC="yes" \
     iwconfig
