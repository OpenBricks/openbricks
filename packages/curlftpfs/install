#!/bin/sh

. config/options

$SCRIPTS/install GLib
$SCRIPTS/install curl
$SCRIPTS/install libiconv

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/src/curlftpfs $INSTALL/usr/bin
cp $PACKAGES/$1/scripts/automount_ftp $INSTALL/usr/bin

mkdir -p $INSTALL/etc
cp $PACKAGES/config/ftpmounts $INSTALL/etc
