#!/bin/sh
#
# start HAL daemon
#
# runlevels: geexbox, debug, configure

if test -f /usr/bin/hald; then
  echo "### Starting Hardware Abstraction Layer ###"
  hald --verbose=no --daemon=yes
fi

exit 0
