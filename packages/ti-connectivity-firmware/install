#!/bin/sh

. config/options

mkdir -p $INSTALL/firmware/ti-connectivity
cp -P $BUILD/linux-firmware-r*/ti-connectivity/* $INSTALL/firmware/ti-connectivity
chmod -x $INSTALL/firmware/ti-connectivity/*
