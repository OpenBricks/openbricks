#!/bin/sh

. config/options

$SCRIPTS/unpack linux

HEADERS=$SYSROOT_PREFIX/usr/include
mkdir -p $HEADERS

for d in linux asm-generic asm `readlink $(kernel_path)/include/asm`; do
  cp -pPR $(kernel_path)/include/$d $HEADERS/
done
