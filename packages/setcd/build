#!/bin/sh

. config/options

$SCRIPTS/unpack linux || exit 1
$SCRIPTS/build toolchain || exit 1

make -C $BUILD/setcd* \
     CC=$CC \
     CFLAGS="$CFLAGS -I$KERNEL/include" \
     setcd && \
exit 0

exit 1
