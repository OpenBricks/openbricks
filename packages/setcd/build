#!/bin/sh

. config/options

$SCRIPTS/unpack linux || exit 1
$SCRIPTS/build toolchain || exit 1

make -C $BUILD/setcd* \
     CC=$TARGET_CC \
     CFLAGS="-Os -I$KERNEL/include" \
     LDFLAGS="-s" \
     setcd && \
exit 0

exit 1
