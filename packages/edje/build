#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build ecore

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-static \
            --disable-shared \
            --disable-edje-cc \

make
make install
