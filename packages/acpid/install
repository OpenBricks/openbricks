#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/acpid $INSTALL/usr/bin
[ "$DEVTOOLS" = yes ] && cp $BUILD/$1*/acpi_listen $INSTALL/usr/bin

mkdir -p $INSTALL/etc/acpi/events
mkdir -p $INSTALL/etc/acpi/scripts
