#!/bin/sh

. config/options

$SCRIPTS/build toolchain

unset INSTALL

cd $BUILD/jpeg*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX
make libjpeg.a
make install-lib
