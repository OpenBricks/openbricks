#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/jpeg* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME && \
make libjpeg.a && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
