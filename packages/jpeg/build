#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

unset INSTALL
CWD=`pwd`

cd $BUILD/jpeg* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX && \
make libjpeg.a && \
make install-lib && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
