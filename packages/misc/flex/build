#!/bin/sh

. config/options

get_meta $1

cd $BUILD/$1*
setup_toolchain target

do_configure \
	--host=i686 \
	--enable-shared \
	--disable-static \
	--disable-rpath \
	--with-gnu-ld

patch -p1 < $ROOT/$PACKAGES/$PKG_SECTION/$1/undeine.patch
make
make_install
