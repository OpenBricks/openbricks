#!/bin/sh

echo "Post install:"

cfgfile=config.inc.php
rootdir=/srv/www/htdocs/phpmyadmin
[ ! -e $rootdir/$cfgfile ] && \
  mv $rootdir/config/$cfgfile $rootdir/ && \
  rmdir $rootdir/config

echo "  Installing new config for Web server ..."
/bin/systemctl restart lighttpd
echo "Done."
