#!/bin/sh

. config/options

$SCRIPTS/install freetype
$SCRIPTS/install libiconv

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/glcdgraphics/*.so* $INSTALL/usr/lib
cp -PR $BUILD/$1*/glcddrivers/*.so* $INSTALL/usr/lib

mkdir -p $INSTALL/etc
cp $BUILD/$1*/graphlcd.conf $INSTALL/etc
