#!/bin/sh

. config/options

require_cxx $1

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/scummvm $INSTALL/usr/bin

mkdir -p $INSTALL/usr/share/scummvm
cp $BUILD/$1*/gui/themes/* $INSTALL/usr/share/scummvm
