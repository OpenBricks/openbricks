#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libpthreads-stubs

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-shared \
            --disable-static \

make
make install
