#!/bin/sh

. config/options

mkdir -p $INSTALLX/$XORG_PATH_MODULES/drivers
cp $BUILD/$1*/src/.libs/ati_drv.so $INSTALLX/$XORG_PATH_MODULES/drivers
cp $BUILD/$1*/src/.libs/radeon_drv.so $INSTALLX/$XORG_PATH_MODULES/drivers

mkdir -p $INSTALLX/$XORG_PATH_MODULES/multimedia
cp $BUILD/$1*/src/.libs/theatre_drv.so $INSTALLX/$XORG_PATH_MODULES/multimedia
cp $BUILD/$1*/src/.libs/theatre200_drv.so $INSTALLX/$XORG_PATH_MODULES/multimedia
cp $BUILD/$1*/src/.libs/theatre_detect_drv.so $INSTALLX/$XORG_PATH_MODULES/multimedia
