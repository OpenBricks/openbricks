#!/bin/sh

. config/options

case "$2" in
  stage1)
    mkdir -p $INSTALL/bin
    cp $BUILD/$1*/mount/mount $INSTALL/bin
  ;;

  stage2)
    mkdir -p $INSTALL/usr/bin
    cp $BUILD/$1*/fdisk/sfdisk $INSTALL/usr/bin
  ;;
esac
