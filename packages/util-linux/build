#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build ncurses

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME
make -C fdisk sfdisk
make -C fdisk cfdisk
make -C lib
make -C mount mount
