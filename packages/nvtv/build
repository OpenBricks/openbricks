#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build pciutils

cd $BUILD/nvtv*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --without-x \
            --without-gtk \
            --with-wx-config=nowx \
            --without-xbox \
            --without-client-backend
make
