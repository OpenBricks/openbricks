#!/bin/sh

. config/options

DFB=`ls -d $BUILD/$1*`

for i in `find $DFB -name Makefile.*`; do
  sed -i -e 's%RUNTIME_SYSROOT}@MODULEDIR@%RUNTIME_SYSROOT}/lib/directfb%' $i \
         -e 's%RUNTIME_SYSROOT}@DATADIR@%RUNTIME_SYSROOT}/share/directfb%' $i
done
