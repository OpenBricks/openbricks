#!/bin/sh

. config/options

cd $BUILD/$1*

if [ -d .install/usr/lib ]; then 
  do_install usr/lib/libcrypto.so*
  do_install usr/lib/libssl.so*
  do_install usr/lib/engines
else
  do_install usr/lib64/libcrypto.so*
  do_install usr/lib64/libssl.so*
  do_install usr/lib64/engines
fi
