#!/bin/sh

. config/options

cd $BUILD/eglibc-*

if pkg_uses $1 minimal; then
  for mod in ISO8859-1 ISO8859-15 UNICODE; do
    do_install usr/lib/gconv/$mod.so
  done
  cp -P $ROOT/$PACKAGES/$1/config/gconv-modules $INSTALL/usr/lib/gconv
else
  do_install usr/lib/gconv
fi

exit 0
