#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build sysfsutils

cd $BUILD/$1*/libcpufreq
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-sysfs \
            --disable-proc \
            --disable-shared
make
make install
