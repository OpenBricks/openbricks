#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/$1*/libcpufreq
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-sysfs \
            --enable-proc \
            --disable-shared
make
make install
