#!/bin/sh

. config/options

$SCRIPTS/install make || exit 1

CWD=`pwd`
unset INSTALL
CC=$LOCAL_CC

cd $BUILD/ccache* && \
./configure --prefix=$ROOT/$TOOLCHAIN && \
make && \
cd $CWD && \
exit 0

cd $CWD
exit 1
