#!/bin/sh

. config/options

$SCRIPTS/install make || exit 1

setup_toolchain host
unset INSTALL
CC=$LOCAL_CC

cd $BUILD/ccache* && \
./configure --prefix=$ROOT/$TOOLCHAIN && \
make && \
exit 0

exit 1
