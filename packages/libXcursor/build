#!/bin/sh

. config/options

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-shared \
            --disable-static \
            --with-icondir=$XORG_PATH_ICONS \
            --with-cursorpath=$XORG_PATH_ICONS \

make
make_install
