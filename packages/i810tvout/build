#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1
$SCRIPTS/build pciutils || exit 1

INCLUDE="-I`ls -d $ROOT/$BUILD/pciutils*/`"
LIBS="-L`ls -d $ROOT/$BUILD/pciutils*/lib`"

CWD=`pwd`
cd $BUILD/i810tvout && \
make CC=$TARGET_CC CFLAGS="-Os -s $INCLUDE $LIBS" && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
