#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1
$SCRIPTS/build pciutils || exit 1

make -C $BUILD/i810tvout* \
     CC=$CC \
     CFLAGS="$CFLAGS" \
     LIBS="$LDFLAGS" \
     i810tvout && \
exit 0

exit 1
