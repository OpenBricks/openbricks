#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/$1*
make OPTFLAGS="$CFLAGS" \
     LDFLAGS="$LDFLAGS" \
     CROSS_COMPILE="$TARGET_PREFIX" \
     USE_LOG=false \
     USE_SELINUX=false \
     DEBUG=false \
     EXTRAS=extras/firmware
     V=true
