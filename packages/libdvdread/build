#!/bin/sh

. config/options

cd $BUILD/$1*
./configure2 --host=$TARGET_NAME \
             --build=$HOST_NAME \
             --prefix=/usr \
             --disable-static \
             --enable-shared \
             --disable-debug \
             --disable-strip \
             --cc=$CC \

make
make_install
