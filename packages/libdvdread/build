#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libdvdcss

cd $BUILD/$1*
./configure2 --host=$TARGET_NAME \
             --build=$HOST_NAME \
             --prefix=$LIB_PREFIX \
             --disable-static \
             --enable-shared \
             --disable-debug \
             --cc=$CC \

make
make install
