#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libvdpau

cd $BUILD/$1*

./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-shared \
            --disable-static \

make
strip_libs src
