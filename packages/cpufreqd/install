#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp $BUILD/$1*/.libs/libsys_acpi.so $INSTALL/usr/lib

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/$1 $INSTALL/usr/bin

mkdir -p $INSTALL/etc
cp $PACKAGES/$1*/config/$1.conf $INSTALL/etc
