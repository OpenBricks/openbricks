#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/cdparanoia* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME && \
make lib CC=$TARGET_CC OPT="-Os -ffast-math" && \
cp interface/cdda_interface.h interface/libcdda_interface.a paranoia && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
