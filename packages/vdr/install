#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/vdr $INSTALL/usr/bin
cp $PACKAGES/$1/scripts/runvdr $INSTALL/usr/bin

mkdir -p $INSTALL/usr/lib/vdr

mkdir -p $INSTALL/usr/share/vdr/locale
cp -a $BUILD/$1*/locale/* $INSTALL/usr/share/vdr/locale

mkdir -p $INSTALL/etc/vdr/plugins
mkdir -p $INSTALL/etc/vdr/plugins.d
cp -a $BUILD/$1*/*.conf $INSTALL/etc/vdr
