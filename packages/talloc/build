#!/bin/sh

. config/options

$SCRIPTS/build toolchain

export ac_cv_path_XSLTPROC=

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared
make
make install
