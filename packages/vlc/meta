PKG_NAME=vlc
PKG_VERSION=1.1.1
PKG_URL="http://sourceforge.net/projects/vlc/files/$PKG_VERSION/vlc-$PKG_VERSION.tar.bz2"
PKG_REV=1
PKG_BUILD_DEPENDS="toolchain zlib ffmpeg freetype alsa-lib faad2 libpng dbus libcddb mpc fontconfig libxml2 udev lirc libgcrypt gnutls sqlite v4l-utils libdvbpsi libcdio"
PKG_DEPENDS="$TARGET_LIBC zlib ffmpeg freetype alsa-lib libpng faad2 dbus libcddb fontconfig libxml2 udev lirc libgcrypt gnutls sqlite v4l-utils libdvbpsi libcdio"
if [ "$SDL" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS SDL"
  PKG_DEPENDS="$PKG_DEPENDS SDL"
fi
if [ "$XORG" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS libX11 libXv libxcb xcb-utils libXxf86vm mesa"
  PKG_DEPENDS="$PKG_DEPENDS libX11 libXv libXxf86vm libxcb xcb-utils mesa"
fi
if [ "$LIVE555" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS live"
fi
if [ "$OPENMAX" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS libomxil-bellagio"
  PKG_DEPENDS="$PKG_DEPENDS libomxil-bellagio"
fi
if [ "$DIRECTFB" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS DirectFB"
  PKG_DEPENDS="$PKG_DEPENDS DirectFB"
fi
if [ "$PULSEAUDIO" = yes ]; then
  PKG_BUILD_DEPENDS="$PKG_BUILD_DEPENDS pulseaudio"
  PKG_DEPENDS="$PKG_DEPENDS pulseaudio"
fi
PKG_PRIORITY=optional
PKG_SECTION=misc
PKG_SHORTDESC="VideoLAN multimedia player and streamer"
PKG_LONGDESC="VLC is the VideoLAN project's media player. It plays MPEG, MPEG2, MPEG4, DivX, MOV, WMV, QuickTime, mp3, Ogg/Vorbis files, DVDs, VCDs, and multimedia streams from various network sources."
