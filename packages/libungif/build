#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/libungif* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --without-x && \
make -C lib && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
