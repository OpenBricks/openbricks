#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/libungif*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared \
            --without-x
make -C lib
make -C lib install
