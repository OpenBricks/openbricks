#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`
export CC=$TARGET_CC
export CFLAGS="-Os -s"

cd $BUILD/libungif* && \
./configure --host=$TARGET_NAME \
            --without-x && \
make -C lib && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
