#!/bin/sh

# Use X.Org 7.3+ capabilities to auto-detect hardware and
# create its configuration file accordingly.

CFG_FILE=/etc/X11/xorg.conf

Xorg -configure -probeonly -br
cp /xorg.conf.new $CFG_FILE
