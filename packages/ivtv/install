#!/bin/sh

. config/options

IVTV_DIR="`ls -d $INSTALL/lib/modules/*`/kernel/drivers/media/video/ivtv"

mkdir -p $IVTV_DIR
cp $BUILD/$1*/driver/ivtv.ko $IVTV_DIR
cp $PACKAGES/$1/config/pvr $INSTALL/etc
