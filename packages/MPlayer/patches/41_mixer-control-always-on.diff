diff -r 4cec08e0f003 mplayer.c
--- a/mplayer.c	Sun Apr 30 16:54:32 2006 +0200
+++ b/mplayer.c	Sun Apr 30 16:54:33 2006 +0200
@@ -3597,6 +3597,16 @@ if(sh_audio){
   }
   mixer.audio_out = audio_out;
   mixer.volstep = volstep;
+}
+else if (!strncmp (filename, "dvd", 3) || strcasestr (filename, ".ifo")) {
+  audio_out=NULL;
+}
+else{
+    if(audio_out=init_best_audio_out(audio_driver_list,0,11025,2,9,0)){
+        inited_flags|=INITED_AO;
+        mixer.audio_out = audio_out;
+        mixer.volstep = volstep;
+    }
 }
 
 current_module="av_init";
