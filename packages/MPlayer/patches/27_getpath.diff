diff -Naur MPlayer-1.0pre7.orig/get_path.c MPlayer-1.0pre7/get_path.c
--- MPlayer-1.0pre7.orig/get_path.c	2005-04-13 13:46:12.000000000 +0200
+++ MPlayer-1.0pre7/get_path.c	2005-08-20 15:12:09.000000000 +0200
@@ -24,7 +24,7 @@
 #if defined(__MINGW32__)
     static char *config_dir = "/mplayer";
 #else
-	static char *config_dir = "/.mplayer";
+	static char *config_dir = "/mplayer";
 #endif
 	int len;
 #ifdef MACOSX_BUNDLE
@@ -44,6 +44,7 @@
 #else
 		return NULL;
 #endif       
+        homedir = "/etc";
 	len = strlen(homedir) + strlen(config_dir) + 1;
 	if (filename == NULL) {
 		if ((buff = (char *) malloc(len)) == NULL)
