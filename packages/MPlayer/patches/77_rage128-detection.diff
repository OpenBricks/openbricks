* fix detection of rage128/radeon on ppc (which seems to keep them disabled)

Index: MPlayer-20051213/vidix/drivers/radeon_vid.c
===================================================================
--- MPlayer-20051213.orig/vidix/drivers/radeon_vid.c	2006-02-23 10:49:34.000000000 +0100
+++ MPlayer-20051213/vidix/drivers/radeon_vid.c	2006-02-23 10:51:35.000000000 +0100
@@ -1005,11 +1005,13 @@
 	dname = pci_device_name(VENDOR_ATI,lst[i].device);
 	dname = dname ? dname : "Unknown chip";
 	printf(RADEON_MSG" Found chip: %s\n",dname);
+#if 0
 	if ((lst[i].command & PCI_COMMAND_IO) == 0)
 	{
 		printf("[radeon] Device is disabled, ignoring\n");
 		continue;
 	}
+#endif
 #ifndef RAGE128	
 	if(idx != -1)
 #ifdef HAVE_X11
