diff -Naur MPlayer-20050806.orig/cfg-common.h MPlayer-20050806/cfg-common.h
--- MPlayer-20050806.orig/cfg-common.h	2005-09-14 22:57:55.000000000 +0200
+++ MPlayer-20050806/cfg-common.h	2005-09-15 22:57:28.000000000 +0200
@@ -22,7 +22,8 @@
 #endif
 	{"vcd", "-vcd N is deprecated, use vcd://N instead.\n", CONF_TYPE_PRINT, CONF_NOCFG ,0,0, NULL},
 	{"cuefile", "-cuefile is deprecated, use cue://filename:N where N is the track number.\n", CONF_TYPE_PRINT, 0, 0, 0, NULL},
-	{"cdrom-device", &cdrom_device, CONF_TYPE_STRING, 0, 0, 0, NULL},
+	{"vcd-device", &vcd_device, CONF_TYPE_STRING, 0, 0, 0, NULL},
+	{"cdda-device", &cdda_device, CONF_TYPE_STRING, 0, 0, 0, NULL},
 #ifdef USE_DVDNAV
 	{"dvdnav", "-dvdnav is deprecated, use dvdnav:// instead.\n", CONF_TYPE_PRINT, 0, 0, 1, NULL},
 	{"skipopening", &dvd_nav_skip_opening, CONF_TYPE_FLAG, 0, 0, 1, NULL},
@@ -434,7 +435,7 @@
 extern int vivo_param_width;
 extern int vivo_param_height;
 extern int vivo_param_vformat;
-extern char *dvd_device, *cdrom_device;
+extern char *dvd_device, *vcd_device, *cdda_device;
 
 m_option_t vivoopts_conf[]={
 	{"version", &vivo_param_version, CONF_TYPE_INT, 0, 0, 0, NULL},
diff -Naur MPlayer-20050806.orig/libmpdemux/cdda.c MPlayer-20050806/libmpdemux/cdda.c
--- MPlayer-20050806.orig/libmpdemux/cdda.c	2005-09-15 23:02:04.000000000 +0200
+++ MPlayer-20050806/libmpdemux/cdda.c	2005-09-15 23:01:59.000000000 +0200
@@ -13,7 +13,7 @@
 
 #include "cdd.h"
 
-extern char *cdrom_device;
+char *cdda_device;
 
 static struct cdda_params {
   int speed;
@@ -105,8 +105,8 @@
   }
 
   if(!p->device) {
-    if (cdrom_device)
-      p->device = strdup(cdrom_device);
+    if (cdda_device)
+      p->device = strdup(cdda_device);
     else
       p->device = strdup(DEFAULT_CDROM_DEVICE);
   }
diff -Naur MPlayer-20050806.orig/libmpdemux/stream_vcd.c MPlayer-20050806/libmpdemux/stream_vcd.c
--- MPlayer-20050806.orig/libmpdemux/stream_vcd.c	2005-09-14 22:58:01.000000000 +0200
+++ MPlayer-20050806/libmpdemux/stream_vcd.c	2005-09-15 22:59:40.000000000 +0200
@@ -25,7 +25,7 @@
 #include "vcd_read.h"
 #endif
 
-extern char *cdrom_device;
+char *vcd_device;
 
 static struct stream_priv_s {
   int track;
@@ -82,8 +82,8 @@
   }
 
   if (!p->device) {
-    if(cdrom_device)
-      p->device = strdup(cdrom_device);
+    if(vcd_device)
+      p->device = strdup(vcd_device);
     else
       p->device = strdup(DEFAULT_CDROM_DEVICE);
   }
diff -Naur MPlayer-20050806.orig/mplayer.c MPlayer-20050806/mplayer.c
--- MPlayer-20050806.orig/mplayer.c	2005-09-15 23:02:04.000000000 +0200
+++ MPlayer-20050806/mplayer.c	2005-09-15 23:03:44.000000000 +0200
@@ -2929,7 +2929,7 @@
       play_tree_t *cdda, *tracks;
       int nb_tracks, i; 
 
-      nb_tracks = get_cdda_tracks_num (cdrom_device ? cdrom_device : DEFAULT_CDROM_DEVICE);
+      nb_tracks = get_cdda_tracks_num (cdda_device ? cdda_device : DEFAULT_CDROM_DEVICE);
       if (nb_tracks <= 0)
         break;
 
