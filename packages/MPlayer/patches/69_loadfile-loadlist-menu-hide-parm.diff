diff -Naur MPlayer-20051213.orig/input/input.c MPlayer-20051213/input/input.c
--- MPlayer-20051213.orig/input/input.c	2005-12-14 20:12:34.000000000 +0100
+++ MPlayer-20051213/input/input.c	2005-12-14 19:58:16.000000000 +0100
@@ -121,8 +121,8 @@
   { MP_CMD_SCREENSHOT, "screenshot", 0, { {-1,{0}} } },
   { MP_CMD_PANSCAN, "panscan",1,  { {MP_CMD_ARG_FLOAT,{0}}, {MP_CMD_ARG_INT,{0}}, {-1,{0}} } },
   { MP_CMD_SWITCH_VSYNC, "switch_vsync", 0, { {MP_CMD_ARG_INT,{0}}, {-1,{0}} } },
-  { MP_CMD_LOADFILE, "loadfile", 1, { {MP_CMD_ARG_STRING, {0}}, {MP_CMD_ARG_INT,{0}}, {-1,{0}} } },
-  { MP_CMD_LOADLIST, "loadlist", 1, { {MP_CMD_ARG_STRING, {0}}, {MP_CMD_ARG_INT,{0}}, {-1,{0}} } },
+  { MP_CMD_LOADFILE, "loadfile", 1, { {MP_CMD_ARG_STRING, {0}}, {MP_CMD_ARG_INT,{0}}, {MP_CMD_ARG_STRING, {0}}, {-1,{0}} } },
+  { MP_CMD_LOADLIST, "loadlist", 1, { {MP_CMD_ARG_STRING, {0}}, {MP_CMD_ARG_INT,{0}}, {MP_CMD_ARG_STRING, {0}}, {-1,{0}} } },
   { MP_CMD_RUN, "run", 1, { {MP_CMD_ARG_STRING,{0}}, {-1,{0}} } },
   { MP_CMD_VF_CHANGE_RECTANGLE, "change_rectangle", 2, { {MP_CMD_ARG_INT,{0}}, {MP_CMD_ARG_INT,{0}}, {-1,{0}}}},
 
diff -Naur MPlayer-20051213.orig/mplayer.c MPlayer-20051213/mplayer.c
--- MPlayer-20051213.orig/mplayer.c	2005-12-14 20:12:34.000000000 +0100
+++ MPlayer-20051213/mplayer.c	2005-12-14 20:13:21.000000000 +0100
@@ -3486,6 +3486,8 @@
       play_tree_t* e = play_tree_new();
       play_tree_add_file(e,cmd->args[0].v.s);
 
+      if (cmd->nargs>=3 && !strcmp(cmd->args[2].v.s, "hide_menu"))
+        mp_input_queue_cmd(mp_input_parse_cmd("menu hide"));
       if (cmd->args[1].v.i) // append
         play_tree_append_entry(playtree, e);
       else {
@@ -3504,6 +3506,8 @@
       if(!e)
 	mp_msg(MSGT_CPLAYER,MSGL_ERR,MSGTR_PlaylistLoadUnable,cmd->args[0].v.s);
       else {
+	if (cmd->nargs>=3 && !strcmp(cmd->args[2].v.s, "hide_menu"))
+	  mp_input_queue_cmd(mp_input_parse_cmd("menu hide"));
 	if (cmd->args[1].v.i) // append
 	  play_tree_append_entry(playtree, e);
 	else {
