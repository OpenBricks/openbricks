Index: MPlayer-20051213/input/input.c
===================================================================
--- MPlayer-20051213.orig/input/input.c	2006-02-23 10:51:58.000000000 +0100
+++ MPlayer-20051213/input/input.c	2006-02-23 10:52:04.000000000 +0100
@@ -602,6 +602,7 @@
   int pausing = 0;
   char *ptr,*e;
   mp_cmd_t *cmd, *cmd_def;
+  mp_cmd_t *cmd_ret = NULL;
 
 #ifdef MP_DEBUG
   assert(str != NULL);
@@ -618,6 +619,8 @@
     str = &str[15];
   }
 
+ do {
+
   for(ptr = str ; ptr[0] != '\0'  && ptr[0] != '\t' && ptr[0] != ' ' ; ptr++)
     /* NOTHING */;
   if(ptr[0] != '\0')
@@ -649,6 +652,11 @@
     ptr = strchr(ptr,' ');
     if(!ptr) break;
     while(ptr[0] == ' ' || ptr[0] == '\t') ptr++;
+    if (ptr[0] == ';')
+      {
+        str = ptr + 1;
+        break;
+      }
     if(ptr[0] == '\0') break;
     cmd->args[i].type = cmd_def->args[i].type;
     switch(cmd_def->args[i].type) {
@@ -727,7 +735,14 @@
   if(i < MP_CMD_MAX_ARGS)
     cmd->args[i].type = -1;
 
-  return cmd;
+  if(!cmd_ret)
+    cmd_ret = cmd;
+  else
+    mp_input_queue_cmd(cmd);
+
+ } while (ptr && ptr[0] == ';');
+
+  return cmd_ret;
 }
 
 static int
