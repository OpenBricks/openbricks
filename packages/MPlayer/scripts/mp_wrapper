#!/bin/sh
[ -f /tmp/widescreen ] && BACKGROUND=/usr/share/mplayer/background-wide.avi || BACKGROUND=/usr/share/mplayer/background.avi

mplayer $BACKGROUND -loop 0 >/dev/null 2>&1
echo $? > /tmp/mp_result
