#!/bin/sh

device=${1:-/dev/dvd}

if [ -e /var/dvdnav ]; then
  echo -e "menu hide\nloadfile \"dvdnav:///$device\"" > /var/mp_control
else
  echo -e "menu hide\nloadfile \"dvd:///$device\"" > /var/mp_control
fi
