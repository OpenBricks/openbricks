#!/bin/sh

. config/options

export LIBS="-L$SYSROOT_PREFIX/usr/lib"

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-shared \
            --disable-static \
            --with-system-ffmpeg \

make
strip_libs ext
make_install
