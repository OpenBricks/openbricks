#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/bin
cp $PACKAGES/$1/scripts/geexbox-stats.sh $INSTALL/usr/bin/

mkdir -p $INSTALL/etc
[ "$STATS_ENABLED" = yes ] && echo -n "" > $INSTALL/etc/stats

exit 0
