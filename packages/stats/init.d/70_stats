#!/bin/sh
#
# Provides system statistics to GeeXboX project
#
# runlevels: geexbox, debug

[ -f /etc/stats -a -f /etc/network ] || exit 1

echo "### System Statistics ###"
. /etc/network.scripts

wait_for_network

/usr/bin/geexbox-stats.sh

exit 0
