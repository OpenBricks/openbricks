#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libvdpau

require_cxx $1

cd $BUILD/$1*
./configure    --host=$TARGET_NAME \
               --build=$HOST_NAME \
               --libdir=$LIB_PREFIX/lib
make
