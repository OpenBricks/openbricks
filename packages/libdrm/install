#!/bin/sh

. config/options

cd $BUILD/$1*

do_install usr/lib/libdrm.so*
do_install usr/lib/libkms.so*
for card in intel radeon nouveau; do
  pkg_uses $1 $card && do_install usr/lib/libdrm_${card}.so*
done

exit 0
