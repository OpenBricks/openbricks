#!/bin/sh

. config/path
. $CONFIG/options


$SCRIPTS/build bftpd || exit 1
$SCRIPTS/build samba || exit 1

if [ "$WIFI" = yes ]; then
  $SCRIPTS/build wireless_tools || exit 1;
  $SCRIPTS/build at76c503 || exit 1;
fi

exit 0
