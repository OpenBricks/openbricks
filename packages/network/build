#!/bin/sh

. config/options

$SCRIPTS/build bftpd || exit 1
$SCRIPTS/build samba || exit 1

if [ "$WIFI" = yes ]; then
  $SCRIPTS/build wireless_tools || exit 1;
  $SCRIPTS/build rt2400 || exit 1;
  $SCRIPTS/build RT2500 || exit 1;
fi

exit 0
