#!/bin/sh

. config/options

make -C $BUILD/$1*/$1 install

[ "$2" = "toolchain" ] || ( mkdir -p $INSTALL/bin && cp $BUILD/$1-*/$1/$1 $INSTALL/bin )
#[ -n "$INSTALL" ] && ( mkdir -p $INSTALL/bin && cp $BUILD/$1-*/$1/$1 $INSTALL/bin )

