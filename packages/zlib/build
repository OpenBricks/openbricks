#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

unset INSTALL
AR="$AR rc"

cd $BUILD/zlib* && \
./configure --prefix=$LIB_PREFIX \
            --shared && \
make && \
make libz.a && \
make install && \
exit 0

exit 1
