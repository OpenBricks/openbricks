#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

unset INSTALL
AR="$AR rc"

cd $BUILD/zlib* && \
./configure && \
make && \
./configure -s --prefix=$LIB_PREFIX && \
make && \
make install && \
exit 0

exit 1
