#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

unset INSTALL
CWD=`pwd`
AR="$AR rc"

cd $BUILD/zlib* && \
./configure && \
make && \
./configure -s --prefix=$LIB_PREFIX && \
make && \
make install && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
