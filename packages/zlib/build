#!/bin/sh

. config/options

export MAKEFLAGS=-j1

cd $BUILD/$1*
./configure --prefix=/usr \
            --shared \
            --uname=Linux

make
make_install

# remove static library
rm -f .install/usr/lib/libz.a
