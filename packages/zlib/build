#!/bin/sh

. config/options toolchain

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`
AR="$AR rc"

cd $BUILD/zlib* && \
./configure && \
make && \
./configure -s && \
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
