#!/bin/sh

. config/options

$SCRIPTS/build toolchain

export MAKEFLAGS=-j1

cd $BUILD/$1*
./configure --prefix=$LIB_PREFIX \
            --shared \
            --uname=Linux

make
strip_libs .
make install
