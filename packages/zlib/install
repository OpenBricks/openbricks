#!/bin/sh

. config/options

$SCRIPTS/build MPlayer
$SCRIPTS/build fbi

SO=`ls -d $BUILD/zlib*`/libz.so.1
$SCRIPTS/libstrip $SO $BUILD/zlib*/libz.a "$BUILD/MPlayer*/mplayer $BUILD/fbi*/fbi"

mkdir -p $INSTALL/usr/lib
cp $SO $INSTALL/usr/lib
