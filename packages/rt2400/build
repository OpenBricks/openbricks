#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/unpack linux

export CROSS_COMPILE=$TARGET_PREFIX
export ARCH=$TARGET_ARCH

cd $BUILD/rt2400*/Module
make KERNDIR=$KERNEL \
     PATCHLEVEL=6
