#!/bin/sh

. config/path

$SCRIPTS/unpack linux || exit 1

CWD=`pwd`

cd "`ls -d $BUILD/rt2400-*`/Module" && \
make KERNEL_SOURCES="`ls -d $ROOT/$BUILD/linux-*`" KERNEL_HEADERS="`ls -d $ROOT/$BUILD/linux-*`/include" PATCHLEVEL=4 && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
