#!/bin/sh

. config/options

require_glibc $1

$SCRIPTS/build toolchain

EGLIBC_DIR=`basename $BUILD/eglibc-2*`

cd $BUILD/$1*

setup_toolchain host
./configure --with-glibc="$ROOT/$BUILD/$EGLIBC_DIR"
make
cp -f localedef $ROOT/$TOOLCHAIN/bin
