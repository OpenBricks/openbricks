--- vdr-vompserver-0.4-0rc/Makefile.org	2013-02-24 19:31:18.291045138 +0100
+++ vdr-vompserver-0.4-0rc/Makefile	2013-02-24 19:32:01.111045871 +0100
@@ -24,12 +24,12 @@
 TMPDIR ?= /tmp
 
 # VOMP INSERT for older VDRs
-ifeq ($(VDRDIR),)
-  VDRDIR = ../../..
-endif
-ifeq ($(LIBDIR),)
-  LIBDIR = ../../lib
-endif
+#ifeq ($(VDRDIR),)
+#  VDRDIR = ../../..
+#endif
+#ifeq ($(LIBDIR),)
+#  LIBDIR = ../../lib
+#endif
 
 APIVERSNUM = $(shell grep 'define APIVERSNUM ' $(VDRDIR)/config.h | awk '{ print $$3 }' | sed -e 's/"//g')
 DOOLDINSTALL =
@@ -52,9 +52,9 @@
 
 APIVERSION = $(call PKGCFG,apiversion)
 # VOMP INSERT for older VDRs
-ifeq ($(APIVERSION),)
-  APIVERSION = $(shell sed -ne '/define APIVERSION/s/^.*"\(.*\)".*$$/\1/p' $(VDRDIR)/config.h)
-endif
+#ifeq ($(APIVERSION),)
+#  APIVERSION = $(shell sed -ne '/define APIVERSION/s/^.*"\(.*\)".*$$/\1/p' $(VDRDIR)/config.h)
+#endif
 # end VOMP INSERT
 
 ### Allow user defined options to overwrite defaults:
