#!/bin/sh

. config/options
get_meta $1

cd $PKG_BUILD_DIR
#do_configure
./configure  --host=$TARGET_NAME \
             --build=$HOST_NAME \
             --prefix=/usr \
             --enable-static \
             --disable-debug \

make
make_install
