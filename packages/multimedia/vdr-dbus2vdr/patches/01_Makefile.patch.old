--- vdr-dbus2vdr-1.0-r006b7b9092/Makefile.old	2013-03-24 11:04:20.000000000 +0100
+++ vdr-dbus2vdr-1.0-r006b7b9092/Makefile	2013-03-30 23:27:20.922809424 +0100
@@ -28,8 +28,8 @@
 ### The compiler options:
 
 export CFLAGS   = $(call PKGCFG,cflags)
-export CXXFLAGS = $(call PKGCFG,cxxflags) `pkg-config --cflags dbus-1` `libpng-config --cflags`
-export LDADD    += `pkg-config --libs dbus-1` `libpng-config --ldflags`
+export CXXFLAGS = -lintl $(call PKGCFG,cxxflags) `pkg-config --cflags dbus-1` `libpng-config --cflags`
+export LDADD    += -lintl `pkg-config --libs dbus-1` `libpng-config --ldflags`
 
 ### The version number of VDR's plugin API:
 
