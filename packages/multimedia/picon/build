#!/bin/sh

. config/options
get_meta $1
mkdir -p $PKG_BUILD_DIR
#mkdir -p $BUILD/picon-1.1
#mkdir -p ${BUILD}-${PKG_VERSION}
#mkdir -p $BUILD/'$1'-'$PKG_VERSION'
cd $PKG_BUILD_DIR
#cd $BUILD/$1*

tar jxf $ROOT/$PACKAGES/$PKG_SECTION/$1/picon/picon.tar.bz2

#do_configure
#make
#make_install
