--- a/CMakeLists.txt	2016-03-12 17:53:43.000000000 +0100
+++ b/CMakeLists.txt	2016-03-12 17:56:22.189530390 +0100
@@ -33,13 +33,6 @@
   add_custom_target(generate ALL DEPENDS ${SHADER_INCLUDES})
   set(SPECTRUM_SOURCES src/directx_spectrum.cpp)
 else(WIN32)
-  find_package(OpenGL)
-  if(OPENGL_FOUND)
-    set(INCLUDES ${OpenGL_INCLUDE_DIR})
-    add_definitions(-DHAS_OPENGL)
-    set(SPECTRUM_SOURCES src/opengl_spectrum.cpp)
-    set(DEPLIBS ${OPENGL_LIBRARIES})
-  else()
     find_package(OpenGLES2)
     if(OPENGLES2_FOUND)
       set(INCLUDES ${OPENGLES2_INCLUDE_DIR})
@@ -50,7 +43,14 @@
                            src/VisShader.cpp)
       set(DEPLIBS ${OPENGLES2_LIBRARIES})
     else()
-      message(FATAL_ERROR "No rendering API found. Bailing")
+      find_package(OpenGL)
+      if(OPENGL_FOUND)
+        set(INCLUDES ${OpenGL_INCLUDE_DIR})
+        add_definitions(-DHAS_OPENGL)
+        set(SPECTRUM_SOURCES src/opengl_spectrum.cpp)
+        set(DEPLIBS ${OPENGL_LIBRARIES})
+      else()
+        message(FATAL_ERROR "No rendering API found. Bailing")
     endif()
   endif()
 endif()
