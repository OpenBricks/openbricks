diff -Naur a/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.cpp b/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.cpp
--- a/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.cpp	2016-08-28 14:01:45.000000000 +0200
+++ b/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.cpp	2016-08-29 22:03:54.000000000 +0200
@@ -34,14 +34,15 @@
 
 CRendererIMX::CRendererIMX()
 {
-  m_bufHistory.clear();
+  m_bufHistory[0] = m_bufHistory[1] = nullptr;
   g_IMXContext.Clear();
 }
 
 CRendererIMX::~CRendererIMX()
 {
   UnInit();
-  std::for_each(m_bufHistory.begin(), m_bufHistory.end(), Release);
+  SAFE_RELEASE(m_bufHistory[0]);
+  SAFE_RELEASE(m_bufHistory[1]);
   g_IMXContext.Clear();
   g_IMX.Deinitialize();
 }
@@ -148,16 +149,10 @@
   CDVDVideoCodecIMXBuffer *buffer = static_cast<CDVDVideoCodecIMXBuffer*>(m_buffers[m_iYV12RenderBuffer].hwDec);
   if (buffer)
   {
-    if (!m_bufHistory.empty() && m_bufHistory.back() != buffer || m_bufHistory.empty())
-    {
-      buffer->Lock();
-      m_bufHistory.push_back(buffer);
-    }
-    if (m_bufHistory.size() > 2)
-    {
-      m_bufHistory.front()->Release();
-      m_bufHistory.pop_front();
-    }
+    SAFE_RELEASE(m_bufHistory[0]);
+    m_bufHistory[0] = m_bufHistory[1];
+    m_bufHistory[1] = buffer;
+    buffer->Lock();
 
     // this hack is needed to get the 2D mode of a 3D movie going
     RENDER_STEREO_MODE stereo_mode = g_graphicsContext.GetStereoMode();
@@ -208,8 +203,7 @@
       }
     }
 
-    CDVDVideoCodecIMXBuffer *buffer_p = m_bufHistory.front();
-    g_IMXContext.Blit(buffer_p == buffer ? nullptr : buffer_p, buffer, fieldFmt);
+    g_IMXContext.Blit(m_bufHistory[0], buffer, fieldFmt);
   }
 
 #if 0
diff -Naur a/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.h b/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.h
--- a/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.h	2016-08-28 14:01:45.000000000 +0200
+++ b/xbmc/cores/VideoPlayer/VideoRenderers/HwDecRender/RendererIMX.h	2016-08-29 22:01:25.000000000 +0200
@@ -63,8 +63,8 @@
   virtual int  GetImageHook(YV12Image *image, int source = AUTOSOURCE, bool readonly = false);
   virtual bool RenderUpdateVideoHook(bool clear, DWORD flags = 0, DWORD alpha = 255);
 
-  std::deque<CDVDVideoCodecIMXBuffer*> m_bufHistory;
-  static void Release(CDVDVideoCodecIMXBuffer *&t) { if (t) t->Release(); }
+private:
+  CDVDVideoCodecIMXBuffer *m_bufHistory[2];
 };
 
 #endif
