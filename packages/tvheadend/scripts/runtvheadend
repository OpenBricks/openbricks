#!/bin/sh

. /etc/pvr.conf

[ "$BACKEND" != tvheadend ] && exit 0

if [ "$WAIT_FIRMWARE" = true ] ; then
  while ! dmesg | grep firmware | grep -q ok ; do
   sleep 2
  done
fi

/usr/bin/tvheadend -c /etc/tvheadend -s
