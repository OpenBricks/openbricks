#!/bin/sh

. config/options

cd $BUILD/$1*

./configure \
            --prefix=/usr \
            --arch=$TARGET_ARCH \
            --cpu=$TARGET_CPU \
            --cc="$TARGET_CC" \
            --release \
            --disable-avahi \

make
make_install
