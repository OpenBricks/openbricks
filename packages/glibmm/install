#!/bin/sh

. config/options

require_cxx $1

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/glib/glibmm/.libs/libglibmm*.so* $INSTALL/usr/lib
cp -PR $BUILD/$1*/gio/giomm/.libs/libgiomm*.so* $INSTALL/usr/lib
