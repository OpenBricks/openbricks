#!/bin/sh

. config/options

$SCRIPTS/install tiff
$SCRIPTS/install libpng
$SCRIPTS/install jpeg

mkdir -p $INSTALL/usr/lib
cp -P $BUILD/$1*/src/lib/.libs/*.so* $INSTALL/usr/lib

mkdir -p $INSTALL/usr/lib/imlib2/loaders
cp -P $BUILD/$1*/src/modules/loaders/.libs/*.so $INSTALL/usr/lib/imlib2/loaders

mkdir -p $INSTALL/usr/lib/imlib2/filters
cp -P $BUILD/$1*/src/modules/filters/.libs/*.so $INSTALL/usr/lib/imlib2/filters
