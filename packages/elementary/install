#!/bin/sh

. config/options

[ "$TARGET_LIBC" = "uClibc" ] && $SCRIPTS/install libiconv
$SCRIPTS/install elementary-theme

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/src/lib/.libs/libelementary*.so* $INSTALL/usr/lib
