#!/bin/sh

. config/options

$SCRIPTS/install libiconv

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/src/lib/.libs/libelementary.so* $INSTALL/usr/lib

mkdir -p $INSTALL/usr/share/elementary/themes
cp -P $BUILD/$1*/data/themes/default.edj $INSTALL/usr/share/elementary/themes
