#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --without-x \
            --with-drivers='all,!Curses' \
            --with-plugins='all,!wireless'
make
