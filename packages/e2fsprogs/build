#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

cd $BUILD/e2fsprogs* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --disable-shared \
            --disable-resizer \
            --disable-imager \
            --disable-debugfs \
            --disable-swapfs && \
make libs && \
make mke2fs -C misc && \
$STRIP misc/mke2fs && \
exit 0

exit 1
