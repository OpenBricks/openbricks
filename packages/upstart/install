#!/bin/sh

. config/options

$SCRIPTS/install dbus
$SCRIPTS/install expat

mkdir -p $INSTALL/sbin
cp -P $BUILD/$1*/init/init $INSTALL/sbin
cp -P $BUILD/$1*/util/initctl $INSTALL/sbin

for i in start stop restart reload status; do
  ln -s initctl $INSTALL/sbin/$i
done

mkdir -p $INSTALL/etc/dbus-1/system.d
cp -P $BUILD/$1*/dbus/Upstart.conf $INSTALL/etc/dbus-1/system.d
