#!/bin/sh

. config/options

cd $BUILD/$1*

do_install sbin/init
do_install sbin/initctl

for i in start stop restart reload status; do
  ln -s initctl $INSTALL/sbin/$i
done

do_install etc/dbus-1/system.d/Upstart.conf

mkdir -p $INSTALL/usr/bin
cp $ROOT/$PACKAGES/$1/scripts/* $INSTALL/usr/bin
