#!/bin/sh

. config/options

mkdir -p $INSTALL/codecs
cp $BUILD/$1*/atrc.so.6.0 $INSTALL/codecs
cp $BUILD/$1*/cook.so $INSTALL/codecs
cp $BUILD/$1*/sipr.so.6.0 $INSTALL/codecs
cp $BUILD/$1*/drvc.so $INSTALL/codecs
cp $BUILD/$1*/wm?9* $INSTALL/codecs
