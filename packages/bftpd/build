#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/bftpd*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --without-debug \
            --without-pax \
            --without-libz \
            --without-pam
make LDFLAGS="$LDFLAGS"
