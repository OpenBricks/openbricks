#!/bin/sh
#
# start ftp server

# get options
test -f /etc/network || exit 1
. /etc/network

test "$FTP_SERVER" = "yes" -a -f /etc/bftpd.conf -a -f /etc/ftp || exit 1
echo "### Starting FTP server ###"
. /etc/ftp
echo "$USERNAME $PASSWORD root /" > /etc/bftpdpwd
exec bftpd -D -c /etc/bftpd.conf
