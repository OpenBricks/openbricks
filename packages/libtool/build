#!/bin/sh

. config/options

cd $BUILD/$1*/libltdl
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-static \
            --disable-shared \
            --enable-ltdl-install \

make 
make_install
rm -rf ../.install
mv .install ..
