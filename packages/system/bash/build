#!/bin/sh

. config/options

get_meta $1
cd $PKG_BUILD_DIR

GCC_NO_LTO=1 GCC_NO_GOLD=1 \
do_configure \
  --prefix=/ \

make
make_install

mkdir -p .install/root
cp $ROOT/$PACKAGES/$PKG_SECTION/$PKG_NAME/config/bashrc .install/root/.bashrc 
