#!/bin/sh

. config/options

$SCRIPTS/build linux

cd $BUILD/$1*
make -C $(kernel_path) SUBDIRS=$PWD modules

MOD_DIR=".install/lib/modules/$(kernel_version)/kernel/fs/exfat"

mkdir -p ${MOD_DIR}
cp -P exfat.ko $MOD_DIR
