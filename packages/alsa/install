#!/bin/sh

. config/options

$SCRIPTS/install alsa-lib
$SCRIPTS/install alsa-utils
[ "$PULSEAUDIO" = yes ] && $SCRIPTS/install alsa-plugins

mkdir -p $INSTALL/etc/modprobe.d
cp $PACKAGES/$1/config/alsa-base.conf $INSTALL/etc/modprobe.d
cp $PACKAGES/$1/config/audio $INSTALL/etc
