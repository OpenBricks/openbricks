#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/grub*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix="`ls -d $ROOT/$BUILD/grub*`/build" \
            --disable-shared \
	    --enable-graphics
make
make install
