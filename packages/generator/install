#!/bin/sh

. config/options

cp $BUILD/$1*/generator.exe $INSTALL
cp $PACKAGES/$1/scripts/generator.sh $INSTALL
cp $PACKAGES/$1/scripts/generator.bat $INSTALL

export INSTALL=$INSTALL/win32
$SCRIPTS/install mkisofs-win32 $2
