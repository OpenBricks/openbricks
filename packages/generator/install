#!/bin/sh

. config/options

$SCRIPTS/install tools-generator

cp $BUILD/$1*/generator.exe $INSTALL
cp $BUILD/$1*/linux-i386-generator $INSTALL
cp $BUILD/$1*/macosx-generator $INSTALL
cp $PACKAGES/$1/scripts/generator.sh $INSTALL
