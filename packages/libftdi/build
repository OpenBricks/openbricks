#!/bin/sh

. config/options

get_meta $1

export ac_cv_path_HAVELIBUSB=$ROOT/$TOOLCHAIN/bin/libusb-config

cd $BUILD/${PKG_NAME}-${PKG_VERSION}
export LIBS="-lusb"
do_configure \
            --disable-libftdipp \

make
make_install
