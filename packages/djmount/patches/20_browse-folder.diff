diff -Naur djmount-0.50.orig/djmount/djfs.c djmount-0.50/djmount/djfs.c
--- djmount-0.50.orig/djmount/djfs.c	2005-12-08 18:30:53.000000000 +0100
+++ djmount-0.50/djmount/djfs.c	2005-12-10 12:03:42.000000000 +0100
@@ -308,13 +308,6 @@
       for (i = 0; i < names->nb; i++) {
 	const char* const devName = names->str[i];
 	DIR_BEGIN(devName) {
-	  FILE_BEGIN("status") {
-	    const char* const str = DeviceList_GetDeviceStatusString 
-	      (tmp_ctx, devName, true);
-	    FILE_SET_STRING (str);
-	    FILE_SET_SIZE (str ? strlen (str) : 0);
-	  } FILE_END;
-	  DIR_BEGIN("browse") {
 	    size_t nb_matched = 0;
 	    const ContentDir_BrowseResult* const res = 
 	      _DJFS_BrowseCDS (tmp_ctx, devName, ptr, &nb_matched);
@@ -369,7 +362,6 @@
 		} PTR_LIST_FOR_EACH_PTR_END;
 	      } DIR_END;
 	    }
-	  } DIR_END; // "browse"
 	} DIR_END; // devName
       }
     }
