#!/bin/sh

#
# UPnP VFS Mounter
#

MOUNT_POINT=/tmp/UPnP

. /etc/network.scripts

# Build UPnP VFS directory
mkdir -p $MOUNT_POINT

wait_for_network

# Mount UPnP VFS
modprobe fuse >/dev/null 2>&1
djmount -f $MOUNT_POINT >/dev/null 2>&1 &

exit 0
