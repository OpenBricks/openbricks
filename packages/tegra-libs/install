#!/bin/sh

. config/options

require_glibc $1

cd $BUILD/$1*

do_install etc
do_install usr/sbin/nvrm_daemon
do_install usr/lib/libnv*.so
do_install usr/lib/libcgdrv.so

if pkg_uses $1 devtools; then
  do_install usr/lib/libPerfHUD*.so
  do_install usr/lib/tegra
  do_install usr/bin
fi

exit 0
