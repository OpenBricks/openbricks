#!/bin/sh

. config/options

[ "$VDR_PLUGINS" = all ] && VDR_PLUGINS="`ls -d $ROOT/$PACKAGES/vdr-* | sed s/.*vdr-//`"

for plugin in $VDR_PLUGINS; do
  [ "$plugin" != plugins ] && $SCRIPTS/install vdr-$plugin
done

exit 0
