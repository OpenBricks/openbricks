#!/bin/sh

. config/path

mkdir -p $INSTALL/usr/share/alsa
cp "`ls -d $BUILD/alsa-lib-*`/src/conf/alsa.conf" $INSTALL/usr/share/alsa
mkdir -p $INSTALL/usr/lib
cp "`ls -d $BUILD/alsa-lib-*`/src/.libs/libasound.so.2" $INSTALL/usr/lib
