#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build xorg-libs
$SCRIPTS/build libXmuu

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX
make
