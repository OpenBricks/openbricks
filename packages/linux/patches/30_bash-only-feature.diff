--- linux-2.6.10.orig/scripts/gen_initramfs_list.sh	Fri Jan 14 10:24:46 2005
+++ linux-2.6.10/scripts/gen_initramfs_list.sh	Fri Jan 14 10:24:25 2005
@@ -51,7 +51,7 @@
 
 parse() {
 	local location="$1"
-	local name="${location/${srcdir}//}"
+	local name="$(echo "$location" | sed -e 's%$srcdir%%')"
 	local mode="$2"
 	local uid="$3"
 	local gid="$4"
