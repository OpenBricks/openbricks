#!/bin/sh
#
# Shutdown procedure (mostly mandatory hacks)
#
# runlevels: geexbox, debug, configure

# Some chipsets using Intel HD-Audio ALSA driver have
# difficulties to properly shutdown.
rmmod snd_hda_intel

exit 0
