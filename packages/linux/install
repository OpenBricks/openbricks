#!/bin/sh

. config/options
get_meta $1

cd $PKG_BUILD_DIR

do_install lib/modules
do_install firmware
do_install boot

if pkg_uses $1 perf ; then
  echo "using perf"
  mkdir -p .install/bin
  cp tools/perf/perf .install/bin
  do_install bin
fi