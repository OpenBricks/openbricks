#!/bin/sh

. config/options

$SCRIPTS/build libspe2

setup_toolchain target

cd $BUILD/$1*
./configure --build=$HOST_NAME \
            --host=$TARGET_NAME \
            --target=$TARGET_NAME \
            --prefix=$LIB_PREFIX \
            --enable-static \
            --disable-shared

make
make install
