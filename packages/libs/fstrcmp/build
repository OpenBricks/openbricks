#!/bin/sh

. config/options

get_meta $1
cd $PKG_BUILD_DIR

GCC_NO_LTO=1 \
    do_configure

rm -f .bin .mk*
make all-bin

rm -rf .install
make DESTDIR=$PWD/.install install-bin install-include install-libdir

sed -e "s:=/usr:=$LIB_PREFIX:g" \
    -i .install/usr/lib/pkgconfig/fstrcmp.pc
