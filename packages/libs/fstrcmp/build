#!/bin/sh

. config/options

get_meta $1
cd $PKG_BUILD_DIR

GCC_NO_LTO=1 do_configure

make all-bin
make DESTDIR=$PWD/.install install-bin install-include install-libdir

sed -e "s%\([=;: ]\|-I\|-L\)/usr%\1$LIB_PREFIX%g" \
    -i .install/usr/lib/pkgconfig/*.pc
