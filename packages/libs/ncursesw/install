#!/bin/sh

. config/options

get_meta $1
cd $PKG_BUILD_DIR

do_install usr/lib/*.so*
do_install usr/lib/terminfo

# !!! terminfo is currently part of the ncurses package !!!
# !!! Do not install it here                            !!!

# file present in ncurses package, do not overwrite
# rm -f .install/usr/share/terminfo/l/linux
# 
# do_install usr/share/t*
