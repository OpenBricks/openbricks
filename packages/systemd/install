#!/bin/sh

. config/options

cd $BUILD/$1*

do_install bin
do_install lib
do_install etc/dbus-1
do_install etc/systemd
do_install usr/bin
do_install usr/share/dbus-1
do_install usr/share/systemd

# add legacy /sbin/init symlink
mkdir -p $INSTALL/sbin
ln -s ../bin/systemd $INSTALL/sbin/init
