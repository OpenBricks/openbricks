#!/bin/sh

. config/options

cd $BUILD/$1*/memmgr

sed -i 's%MEMMGR_CFLAGS="-Werror -pipe -ansi"%MEMMGR_CFLAGS="-pipe -ansi"%' configure

do_configure \
  --enable-tilermgr \
  --disable-tests \
  --disable-stub \

make
make_install

rm -rf ../.install
cp -PR .install ..
