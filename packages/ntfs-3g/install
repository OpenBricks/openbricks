#!/bin/sh

. config/options

[ "$TARGET_LIBC" = "uClibc" ] && $SCRIPTS/install libiconv

mkdir -p $INSTALL/sbin
cp $BUILD/$1*/src/ntfs-3g $INSTALL/sbin/mount.ntfs

mkdir -p $INSTALL/etc
echo "ntfs" >> $INSTALL/etc/filesystems
