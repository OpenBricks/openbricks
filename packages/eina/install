#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/src/lib/.libs/libeina*.so* $INSTALL/usr/lib
mkdir -p $INSTALL/usr/lib/eina/mp
for i in chained_pool pass_through fixed_bitmap; do
  cp -PR $BUILD/$1*/src/modules/mp/$i/.libs/*.so $INSTALL/usr/lib/eina/mp
done
