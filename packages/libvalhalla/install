#!/bin/sh

. config/options

$SCRIPTS/install sqlite
$SCRIPTS/install ffmpeg
$SCRIPTS/install libnfo
$SCRIPTS/install libexif

if [ "$DEVTOOLS" = yes ]; then
  mkdir -p $INSTALL/usr/bin
  cp $BUILD/$1*/libvalhalla-test $INSTALL/usr/bin
fi
