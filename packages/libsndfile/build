#!/bin/sh

. config/options

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-static \
            --disable-shared \
            --disable-alsa \
            --disable-external-libs \
            --disable-sqlite \
            --disable-octave \

make 
make_install
