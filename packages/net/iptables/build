#!/bin/sh

. config/options

DVB_DIR="--with-kernel=$(kernel_path)"
$SCRIPTS/build linux

cd $BUILD/$1*

do_autoconf
do_configure \
            --enable-devel \
            --disable-ipv6 \
	    $DVB_DIR \

make
make_install
