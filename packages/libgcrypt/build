#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libgpg-error

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-static \
            --disable-shared \
            --disable-asm
make
make install
