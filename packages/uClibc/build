#!/bin/sh

. config/options

cd $BUILD/$1*

[ "$DEBUG" = yes ] && CFLAGS="$CFLAGS -D_DLSYM_TRACE_NAME"

make
[ "$DEVTOOLS" = yes ] && make -C utils

installdir="`pwd`/.install"
mkdir -p $installdir
make PREFIX=$installdir install
