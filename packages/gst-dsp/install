#!/bin/sh

. config/options

$SCRIPTS/install ti-dsp

GST_VERSION=0.10
GST_PLUGINS_PATH=$INSTALL/usr/lib/gstreamer-$GST_VERSION

mkdir -p $GST_PLUGINS_PATH
cp -P $BUILD/$1*/libgstdsp.so $GST_PLUGINS_PATH
