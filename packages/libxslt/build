#!/bin/sh

. config/options

cd $BUILD/$1*

do_configure host \
            --with-zlib \
make
make_install toolchain

mkdir -p objdir-host
mv .install objdir-host
make clean

do_configure target \
            --with-zlib \

sed -i "s%^prefix=.*%prefix=$LIB_PREFIX%" xslt-config
make_install
