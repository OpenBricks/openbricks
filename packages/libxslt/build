#!/bin/sh

. config/options

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=/usr \
            --enable-shared \
            --disable-static \
            --with-zlib \

make
sed -i "s%^prefix=.*%prefix=$LIB_PREFIX%" xslt-config
make_install
