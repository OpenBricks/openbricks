#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp -P $( ls $BUILD/$1*/libxslt/.libs/libxslt.so* | grep "libxslt.so[\.0-9]*" ) $INSTALL/usr/lib
cp -P $( ls $BUILD/$1*/libexslt/.libs/libexslt.so* | grep "libexslt.so[\.0-9]*" ) $INSTALL/usr/lib
