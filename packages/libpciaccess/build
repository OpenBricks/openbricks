#!/bin/sh

. config/options

$SCRIPTS/build toolchain

export ac_cv_file__usr_include_asm_mtrr_h=yes

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --enable-shared \
            --disable-static
make
make install
