#!/bin/sh

. config/options

$SCRIPTS/install libxcb

mkdir -p $INSTALL/usr/lib
cp -P $BUILD/$1*/src/.libs/libX11.so*[.0-9] $INSTALL/usr/lib
cp -P $BUILD/$1*/src/.libs/libX11-xcb.so*[.0-9] $INSTALL/usr/lib

mkdir -p $INSTALL/usr/share/X11
cp -P $BUILD/$1*/src/XKeysymDB $INSTALL/usr/share/X11
