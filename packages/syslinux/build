#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1


make -C $BUILD/syslinux* \
     CC=$TARGET_CC \
     CFLAGS="-Os -fomit-frame-pointer -D_FILE_OFFSET_BITS=64" \
     LDFLAGS="-Os -s" \
     pxelinux.0 isolinux.bin syslinux && \
exit 0

exit 1
