#!/bin/sh

. config/options

$SCRIPTS/build toolchain

make -C $BUILD/syslinux* \
     CC=$CC \
     AR=$AR \
     RANLIB=$RANLIB \
     CFLAGS="$CFLAGS -fomit-frame-pointer -D_FILE_OFFSET_BITS=64" \
     LDFLAGS="$LDFLAGS" \
     ldlinux.sys \
     pxelinux.0 \
     isolinux.bin \
     syslinux
