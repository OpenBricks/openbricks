#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/syslinux* && \
make ldlinux.sys pxelinux.0 mbr.bin isolinux.bin syslinux CC=$TARGET_CC CFLAGS="-Os -fomit-frame-pointer -D_FILE_OFFSET_BITS=64" LDFLAGS="-Os -s" && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
