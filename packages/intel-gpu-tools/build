#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build libdrm
$SCRIPTS/build libpciaccess

cd $BUILD/$1*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared \
            --enable-static \

make
