#!/bin/sh
#
# configure and start Enna
#
# runlevels: geexbox, debug

if [ -f /usr/bin/enna ]; then
  echo "### Starting Enna ###"
  mkdir -p /root/.enna
  cp -f /etc/enna.cfg /root/.enna/
  enna -f
fi

exit 0
