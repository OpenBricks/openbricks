#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1
$SCRIPTS/build libogg || exit 1

unset INSTALL

cd $BUILD/libtheora* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-shared \
            --disable-oggtest && \
make && \
make install && \
exit 0

exit 1
