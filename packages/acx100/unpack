#!/bin/sh

. config/path

$SCRIPTS/unpack linux || exit 1

KSRC=`ls -d $ROOT/$BUILD/linux*`

sed -i "s%^KERNEL_BUILD=.*%KERNEL_BUILD=$KSRC%" $BUILD/acx100*/Configure
sed -i "s% -O % -Os %" $BUILD/acx100*/src/Makefile
