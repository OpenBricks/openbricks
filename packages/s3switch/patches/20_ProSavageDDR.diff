--- s3switch-0.0.20030423.orig/s3switch.c
+++ s3switch-0.0.20030423/s3switch.c
@@ -101,6 +105,7 @@
     ioperm( 0x61, 1, enable );
     ioperm( 0x80, 1, enable );
     ioperm( 0x3b0, 0x30, enable );
+    ioperm( 0xeb, 1, enable );
 }
 
 
