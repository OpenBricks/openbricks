#!/bin/sh
#
# start UDEV automounter daemon
#
# runlevels: geexbox, debug, configure

if test -f /usr/bin/automountd; then
  echo "### Starting UDEV automounter ###"
  automountd -d > /dev/null 2>&1
fi

exit 0
