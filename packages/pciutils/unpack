#!/bin/sh

. config/options

sed -i -e "s/sys=.*/sys=Linux/" \
       -e "s/rel=.*/rel=$KERNEL_VERSION/" \
       -e "s/cpu=.*/cpu=$TARGET_ARCH/" \
       `ls -d $BUILD/pciutils*/lib/configure`

ln -s lib `ls -d $BUILD/pciutils*`/pci
