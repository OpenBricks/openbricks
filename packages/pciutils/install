#!/bin/sh

. config/options

$SCRIPTS/build i810tvout
$SCRIPTS/build nvtv

SO=`ls -d $BUILD/pciutils*`/libpci.so.0
libstrip $SO $BUILD/pciutils*/lib/libpci.a "$BUILD/i810tvout*/i810tvout $BUILD/nvtv*/src/nvtv"

mkdir -p $INSTALL/usr/lib
cp $SO $INSTALL/usr/lib
