#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp $BUILD/$1*/lib/libpci.so.0 $INSTALL/usr/lib

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/lspci $INSTALL/usr/bin

mkdir -p $INSTALL/usr/share/misc
cp $BUILD/$1*/pci.ids $INSTALL/usr/share/misc
