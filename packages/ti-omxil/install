#!/bin/sh

. config/options

$SCRIPTS/install libbridge
$SCRIPTS/install libomxil-bellagio

mkdir -p $INSTALL/usr/lib
cp -P $BUILD/$1*/src/*.so $INSTALL/usr/lib
cp -P $BUILD/$1*/src/components/*.so $INSTALL/usr/lib
