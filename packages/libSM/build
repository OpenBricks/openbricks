#!/bin/sh

. config/options

get_meta $1

cd $BUILD/${PKG_NAME}-${PKG_VERSION}

do_configure \
            --disable-ipv6 \
            --without-libuuid \
            --disable-docs \

make
make_install
