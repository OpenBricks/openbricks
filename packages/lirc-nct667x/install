#!/bin/sh

. config/options

mkdir -p "`ls -d $INSTALL/lib/modules/*`/kernel/extra"
cp $BUILD/$1*/lirc_wb677.ko $INSTALL/lib/modules/*/kernel/extra

cp $PACKAGES/$1/config/lircd_nuvoton $INSTALL/etc/lirc

exit 0
