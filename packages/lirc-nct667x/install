#!/bin/sh

. config/options

dir=$INSTALL/lib/modules/$(kernel_version)/kernel/extra

mkdir -p $dir
cp $BUILD/$1*/lirc_wb677.ko $dir

mkdir -p $INSTALL/etc/lirc
cp $PACKAGES/$1/config/lircd_nuvoton $INSTALL/etc/lirc
