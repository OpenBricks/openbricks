# dummy entry

begin
    prog = irexec
    button = KEY_DASHBOARD
    config = systemctl stop vdr-sxfe.service ; systemctl start xbmc.service
    config = systemctl start vdr-sxfe.service ; systemctl stop xbmc.service
end

# Button Zoom
begin
	prog = xine
	remote = *
	button = KEY_ZOOM
	delay = 4
	repeat = 0
	config = z
	config = Z
	flags = toggle_reset
end
