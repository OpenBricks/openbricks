#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp -P $BUILD/$1*/objdir-target/glib/.libs/*.so*[.0-9] $INSTALL/usr/lib/
cp -P $BUILD/$1*/objdir-target/gobject/.libs/*.so*[.0-9] $INSTALL/usr/lib/
cp -P $BUILD/$1*/objdir-target/gmodule/.libs/*.so*[.0-9] $INSTALL/usr/lib/
