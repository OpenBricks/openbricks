#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/objdir-target/glib/.libs/*.so* $INSTALL/usr/lib/
cp -PR $BUILD/$1*/objdir-target/gobject/.libs/*.so* $INSTALL/usr/lib/
cp -PR $BUILD/$1*/objdir-target/gmodule/.libs/*.so* $INSTALL/usr/lib/
