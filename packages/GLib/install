#!/bin/sh

. config/options

mkdir -p $INSTALL/usr/lib
cp -PR $BUILD/$1*/glib/.libs/*.so* $INSTALL/usr/lib/
cp -PR $BUILD/$1*/gobject/.libs/*.so* $INSTALL/usr/lib/
cp -PR $BUILD/$1*/gmodule/.libs/*.so* $INSTALL/usr/lib/
