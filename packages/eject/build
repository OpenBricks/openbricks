#!/bin/sh

. config/options

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`
export CC=$TARGET_CC
export CFLAGS="-Os -s"

cd $BUILD/eject* && \
./configure && \
make && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
