#!/bin/sh

. config/options

$SCRIPTS/install ti-dsp

mkdir -p $INSTALL/usr/lib
cp -P $BUILD/$1*/libbridge/libbridge.so $INSTALL/usr/lib/libbridge.so.2
ln -s libbridge.so.2 $INSTALL/usr/lib/libbridge.so
cp -P $BUILD/$1*/libqos/libqos.so $INSTALL/usr/lib
