#!/bin/sh

. config/options

mkdir -p $INSTALL/sbin
cp $PACKAGES/$1/scripts/$1.sh $INSTALL/sbin/$1
sed -i "s/VERSION=.*/VERSION=$DISTRO_VERSION/" $INSTALL/sbin/$1

mkdir -p $INSTALL/etc/installator
cp $PACKAGES/$1/config/*.install $INSTALL/etc/installator
