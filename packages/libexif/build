#!/bin/sh

. config/options

$SCRIPTS/build toolchain

cd $BUILD/libexif*
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --prefix=$LIB_PREFIX \
            --disable-nls
make -C libexif
make -C libexif install
