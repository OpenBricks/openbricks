#!/bin/sh

. config/options toolchain

$SCRIPTS/build toolchain || exit 1

CWD=`pwd`

cd $BUILD/libexif* && \
./configure --host=$TARGET_NAME \
            --build=$HOST_NAME \
            --disable-nls && \
make -C libexif && \
cd "$CWD" && \
exit 0

cd "$CWD"
exit 1
