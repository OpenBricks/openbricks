#!/bin/sh

. config/options

$SCRIPTS/build toolchain
$SCRIPTS/build signGP

cd $BUILD/$1*

make CROSS_COMPILE=${TARGET_PREFIX} touchbook_config
make CROSS_COMPILE=${TARGET_PREFIX}

signGP x-load.bin
