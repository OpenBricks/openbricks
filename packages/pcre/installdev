#!/bin/sh

. config/options
get_meta $1

cd $BUILD/${PKG_NAME}-${PKG_VERSION}

do_installdev usr/include
do_installdev usr/lib
sed -i "s:prefix=/usr:prefix=$LIB_PREFIX:" .install/usr/bin/pcre-config
do_installdev usr/bin/pcre-config toolchain
