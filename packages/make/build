#!/bin/sh

. config/options

CWD=`pwd`
export CC=$LOCAL_CC
export CFLAGS="-O2"
export LDFLAGS="-s"

cd $BUILD/make* && \
./configure --disable-nls && \
sh build.sh && \
cd $CWD && \
exit 0

cd $CWD
exit 1
