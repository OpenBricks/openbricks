#!/bin/sh

. config/options

mkdir -p "`ls -d $INSTALL/lib/modules/*`/kernel/fs/fuse"
cp $BUILD/$1*/kernel/fuse.ko $INSTALL/lib/modules/*/kernel/fs/fuse

mkdir -p $INSTALL/usr/bin
cp $BUILD/$1*/util/fusermount $INSTALL/usr/bin
