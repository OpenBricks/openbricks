#!/bin/sh

. config/options

mkdir -p "`ls -d $INSTALL/lib/modules/*`/kernel/fs/fuse"
cp $BUILD/fuse*/kernel/fuse.ko $INSTALL/lib/modules/*/kernel/fs/fuse

mkdir -p $INSTALL/usr/bin
cp $BUILD/fuse*/util/fusermount $INSTALL/usr/bin
