#!/bin/sh

. config/options

mkdir -p $INSTALL/etc/systemd/system

# for now, mask this service (we should not install it in the first place)
ln -s /dev/null $INSTALL/etc/systemd/system/hdmi.service

