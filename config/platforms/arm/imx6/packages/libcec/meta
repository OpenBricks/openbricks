PKG_NAME=libcec

# select version
if [ ! "$MEDIA_CENTER_FRONTEND" = kodi ]; then
  PKG_URL_PROTO=git
  PKG_URL_REV=42c3c07a
  PKG_VERSION=2.1.4-2+r${PKG_URL_REV}
  PKG_URL="https://github.com/xbmc-imx6/${PKG_NAME}.git"
fi
