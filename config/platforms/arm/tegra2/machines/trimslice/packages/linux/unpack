#!/bin/sh

. config/options

require_glibc $1
require_cxx $1

get_meta $1

echo "## copy needed files for the kernel"
mkdir -p $BUILD/$1-$PKG_VERSION/firmware/rtl_nic/ >&$VERBOSE_OUT
cp $MACHINE_PACKAGES/$1/firmware/*.fw $PKG_BUILD_DIR/firmware/rtl_nic/ >&$VERBOSE_OUT

./$PACKAGES/$1/unpack $1
