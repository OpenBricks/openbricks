Adding a new architecture
=========================

Architectures are defined in config/plaform. To add support for a new
architecture, start by creating a subdirectory with you arch name and edit the
meta file, using one of the existing architectures as example. After defining
the arch, you should also add generic platform and machine definitions.

// vim: set syntax=asciidoc:
